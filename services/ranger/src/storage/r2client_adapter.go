package storage

// r2client_adapter.go is no longer needed now that *r2.Client directly
// satisfies ObjectClient via the shared storageobjects types and internal
// error mapping helpers in the r2 package.
