apiVersion: v1
kind: Service
metadata:
  name: ranger
  namespace: forestrie-arbor
  labels:
    app: ranger
    component: queue-consumer
spec:
  type: ClusterIP
  ports:
  - port: 9090
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: ranger
