// Package tests contains integration test harnesses for the sealer service.
//
// The actual test files are gated behind build tags (e.g. `integration`) so
// that unit test runs (`go test ./...`) do not require external dependencies
// like MinIO.
package tests


